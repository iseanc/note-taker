- Use NODE.JS as backend
- Use EXPRESS.JS as backend middleware
- Use DB.JSON as database

- ON OPEN, launch /public/index.html (landing page)
- ON CLICKING "Get Started" button on landing page 
    - GO TO Notes page
    - LOAD existing notes in left-hand column
    - PRESENT empty fields to enter 
      - a new note title and
      - the note’s text 
      - in the right-hand column
- ON ADDING A NOTE TITLE & NOTE TEXT, display a SAVE icon in <nav> at top of page
- ON CLICKING SAVE, 
    - Add the note to the database (db.json)
    - Display the note on web-page, left column
- ON CLICKING NOTE IN LEFT COLUMN, 
    - display the note in the right-hand column
- ON CLICKING THE WRITE ICON (in nav bar at top of page)
    - Load a new blank note in the right-hand column

- ON COMPLETION OF APP
    - Deploy app to Heroku.
    - Test all functionality.